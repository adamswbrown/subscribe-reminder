# Supabase — project "subscribe-reminder" (eu-west-2)
# Publishable values are safe for the client; get them from the Supabase dashboard
# (Project Settings → API) or `supabase projects api-keys`.
NEXT_PUBLIC_SUPABASE_URL=https://kdzagbemyzjsrjvkitaw.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Public site origin used in reminder emails (no path).
APP_URL=https://your-deployment.example.com

# Server-only — NEVER commit real values for these.
SUPABASE_SECRET_KEY=            # service-role/secret key, scheduler + server routes only
RESEND_API_KEY=                 # email sending
ANTHROPIC_API_KEY=              # screenshot/PDF import extraction (Claude API)
VAPID_PUBLIC_KEY=               # web push: npx web-push generate-vapid-keys
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
